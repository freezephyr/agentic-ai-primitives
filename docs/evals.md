# Evals

Use `evals.py` to learn the shape of a lightweight evaluation harness.

Each `EvaluationCase` has an input and expected output, and `run_evaluation_suite` turns a runner function into a summary report.
