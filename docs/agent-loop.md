# Agent Loop

Use `agent_loop.py` to study a small plan-act-reflect cycle.

Start with a goal in `AgentContext`, generate `AgentAction` items with a planner, then turn those actions into a final `AgentOutput`.
